# Developing in docker environment
.devcontainer
.vscode

# Frontend
node_modules
package-lock.json # Makes sure people don't use npm install

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
